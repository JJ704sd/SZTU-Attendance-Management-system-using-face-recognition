# =====================================================
# 智能考勤与实验室准入系统 — 主依赖
# =====================================================
# ⚠️  Python 版本要求: 3.10+ (推荐 3.13.x, 已深度验证)
#
# 最低 3.10+ 的原因:
#   - 项目代码用 PEP 604 `X | None` 语法 (需 3.10+)
#   - dlib-bin 20.0.1 PyPI 有 cp311/cp312/cp313 三个 wheel
#   - 3.11 / 3.12 理论能跑 (dlib-bin wheel 全覆盖, 纯 Python 库也全)
#
# 推荐 3.13.x 的原因:
#   - 课程验收机 (Windows 11) + 远端 CI 全程用 3.13
#   - numpy 2.3 + matplotlib 3.10 + PyQt5 5.15 组合在 3.13 上验证过
#   - 188 单元测试 + 8 smoke 端到端全在 3.13 跑过
#
# 装错版本会怎样?
#   - 3.9 及以下: PEP 604 语法直接 SyntaxError, 不能跑
#   - 3.10+ 应该都能装 + 跑 (dlib-bin wheel 全覆盖)
#   - 真要在 3.9 跑, 改 src/ 全部 `X | None` 为 `Optional[X]`
#
# 详细 onboarding 看 QUICKSTART.md 顶部"5 分钟跑起来"
# =====================================================

# --- UI ---
PyQt5==5.15.11

# --- 数据库 ---
SQLAlchemy==2.0.43
PyMySQL==1.1.0
cryptography>=41.0.0

# --- 人脸识别 ---
# dlib-bin 是 dlib 的预编译 wheel（cp311/cp312/cp313 都有），
# 避免源码编译时 cmake + VS Build Tools 的环境成本。
dlib-bin==20.0.1
opencv-python==4.13.0.92

# --- 业务 ---
bcrypt==5.0.0
python-dotenv==1.1.0
numpy==2.3.5
Pillow>=10.0.0
matplotlib==3.10.9  # 3.9+ 起支持 numpy 2.x；3.8.x 仍要求 numpy<2 会和 numpy==2.3.5 冲突

# --- 测试 ---
pytest>=8.0

# --- W14: 多端登录签到 (FastAPI 嵌入) ---
# fastapi 负责 H5 路由 + JSON 响应；uvicorn 是 ASGI server；
# jinja2 渲染 signin.html；httpx 是测试端 client (替代 requests 走同步 API)。
fastapi==0.115.0
uvicorn[standard]==0.32.0
jinja2==3.1.4
httpx==0.27.2

# --- W13+: 二维码签到 (教师端生成二维码图片, 学生扫码进 H5) ---
# qrcode 8.x 是 pre-Python 3.13 wheel 全的最后一个分支; [pil] extra 拉 Pillow 作为后端
# (build.spec hiddenimports 已经显式列 qrcode / qrcode.image.pil / qrcode.image.pure
#  / qrcode.image.base / qrcode.compat.etree / qrcode.constants, 这里必须装否则开发环境 import 失败)
qrcode[pil]==8.2
