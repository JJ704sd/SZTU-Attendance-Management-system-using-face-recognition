# 快速验证 — 5 分钟跑起来

> **接收人**: 老师 / 接手人 / 评审人 / 新组员
> **配套**: README.md (项目总览) + `docs/TEAM_SETUP.md` (9 步精讲 + 跨机上手详细)

本指南**只包含最少步骤**, 5 分钟内必须看到 GUI 启动 + 登录成功。
遇到问题先看 [第 4 节: 故障速查](#4-故障速查)。

---

## 1. 前置条件 (2 分钟)

| 工具 | 版本 | 验证命令 |
|---|---|---|
| **Python** | 3.10+ (推荐 3.13.x) | `python --version` |
| **MySQL** | 8.0.29+ | `mysql --version` |
| **pip** | 23+ | `pip --version` |

**下载**:
- Python: https://www.python.org/downloads/ (勾 "Add to PATH")
- MySQL: https://dev.mysql.com/downloads/installer/ (选 "Developer Default")

---

## 2. 5 步跑起来 (5 分钟)

### 步骤 1: 解压 + 进项目

```powershell
# Windows PowerShell
Expand-Archive .\完整源代码.zip -DestinationPath .
cd attendance-management-system
```

### 步骤 2: 建 .venv + 装依赖 (90s)

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install --upgrade pip
pip install -r requirements.txt
```

> **卡 dlib 装不上?** 看 [第 4 节](#4-故障速查) D1

### 步骤 3: 配 .env (30s)

```powershell
# 复制模板
Copy-Item .env.example .env

# 编辑 .env (记事本 / VSCode)
notepad .env
```

**只改 1 行**: `DB_PASSWORD=YOUR_MYSQL_PASSWORD_HERE` → `DB_PASSWORD=你的MySQL密码`

**MySQL 必先建空库** (命令行):
```sql
mysql -u root -p
> CREATE DATABASE attendance_lab CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
> EXIT;
```

### 步骤 4: 初始化数据库 (30s)

```powershell
python scripts\init_db.py
```

**预期输出**:
```
✓ 14 张表已建好
✓ 5 个 demo 账号已创建
✓ dlib 模型下载 (首次 60s, 跳过模型已存在)
```

### 步骤 5: 启动 GUI (10s)

**双击** `start.bat` ← 最简单

或命令行:
```powershell
python -m src.main
```

**预期**: 弹出"智能考勤与实验室准入系统 v1.0"登录窗口

---

## 3. 演示账号登录 (30s)

| 角色 | 用户名 | 密码 | Tab 数 |
|---|---|---|---|
| 学生 | `test001` | `123456` | 4 (人脸 / 签到 / 考勤 / 请假) |
| 学生 (W14 演示) | `demo_student` | `123456` | (同, student_id=202400502133) |
| 教师 (基础) | `teacher01` | `123456` | 4 (发起 / 历史 / 报表 / 账号) |
| 教师 (W14 演示) | `teacher001` | `123456` | (同, 已挂 BME201 + 2 open task) |
| 管理员 | `admin001` | `123456` | 5 (实验室 / 培训 / 准入 / 报表 / 人脸) |

**3 角色都登录过, 验证:**
- ✅ 学生: 注册新账号 (注册窗口)
- ✅ 教师: 发起考勤任务 (教师端第 1 Tab)
- ✅ 管理员: 看 4 类统计报表 (管理员端第 4 Tab)

---

## 4. 故障速查

### D1: dlib 装不上 (`error: Microsoft Visual C++ 14.0 required`)

**原因**: `dlib` 源码编译需要 MSVC, 但 `dlib-bin` 预编译 wheel 应该跳过
**解决**:
```powershell
pip install dlib-bin==20.0.1
```
如果还是失败, 装 Visual Studio Build Tools 2019+ (选 "C++ 桌面开发")

### D2: `MySQL Connection Refused`

**原因**: MySQL 没启 / 端口不是 3306 / 密码错
**解决**:
```powershell
# 启 MySQL
net start mysql80  # 或 services.msc 找 MySQL 服务名

# 测连接
mysql -u root -p
> SHOW DATABASES;  # 应该能看到 attendance_lab
```

### D3: `ModuleNotFoundError: No module named 'PyQt5'`

**原因**: pip 装到了别的 Python (常见: 全局 Python vs 项目 venv)
**解决**:
```powershell
# 用 venv 里的 pip 显式装
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

### D4: 摄像头打不开 / 黑屏

**原因**: 系统无摄像头 / 摄像头被其他程序占用 (Zoom / 钉钉)
**解决**:
- 关掉所有用摄像头的程序
- 检查 Windows 设置 → 隐私 → 摄像头 → 允许应用访问摄像头
- 真机测试: 笔记本自带摄像头 / USB 摄像头

### D5: `ModuleNotFoundError: No module named 'dlib'` 即使 dlib-bin 装了

**原因**: `dlib-bin` 和 `dlib` 冲突 (同时装)
**解决**:
```powershell
pip uninstall dlib dlib-bin -y
pip install dlib-bin==20.0.1
```

### D6: 启动后白屏 / 控件显示不全

**原因**: Windows 缩放不是 100% / 高 DPI 屏
**解决**:
- 桌面右键 → 显示设置 → 缩放 100% (或 125% 也行)
- 重启程序

### D7: pytest 跑测失败 (`MySQL server has gone away`)

**原因**: 测试用 14 张表, MySQL 连接超时
**解决**:
- 重启 MySQL
- 或调大 `my.ini` 的 `wait_timeout=28800`

### D8: W14 启动时报 "5180 端口被占用"

**原因**: 之前 run 残留 / 其他程序占 5180
**解决**:
- 正常: 自动重试 5181-5184 (W15+ 修)
- 手动: 关掉占用 5180 的程序 (`netstat -ano | findstr :5180`)

---

## 5. 验证清单 (3 分钟, 必跑)

| # | 操作 | 预期 | ✓ |
|---|---|---|---|
| 1 | 启动 GUI | 登录窗口弹出 | ☐ |
| 2 | 学生 `test001/123456` 登录 | 弹出学生 4 Tab | ☐ |
| 3 | 教师 `teacher01/123456` 登录 | 弹出教师 4 Tab | ☐ |
| 4 | 管理员 `admin001/123456` 登录 | 弹出管理员 5 Tab | ☐ |
| 5 | 跑 pytest | `219 passed in ~60s` | ☐ |
| 6 | 跑 smoke_full_flow | `✓ All checks passed` | ☐ |
| 7 | 看 `dist/attendance-system/` | 380 MB exe 产物 (如果含) | ☐ |

**全部 ✓ = 跨机可行性通过**

---

## 6. 详细文档 (按需看)

| 文档 | 看什么 |
|---|---|
| [README.md](README.md) | 项目总览 + 核心亮点 + 3 角色 Tab 矩阵 |
| [docs/TEAM_SETUP.md](docs/TEAM_SETUP.md) | 9 步精讲 (含 .env + MySQL + 防火墙 + dlib 模型下载) |
| [docs/TESTING_CHECKLIST.md](docs/TESTING_CHECKLIST.md) | 10 步亲自测试清单 |
| [docs/CHECKLIST_FOR_REVIEWER.md](docs/CHECKLIST_FOR_REVIEWER.md) | 老师验收 10 步 |
| [docs/HANDOFF.md](docs/HANDOFF.md) | 项目交接总入口 (229 行) |
| [docs/DATABASE.md](docs/DATABASE.md) | 14 张表 schema + 19 FK + 3 UNIQUE |
| [docs/SIGNIN_METHODS.md](docs/SIGNIN_METHODS.md) | 3 种签到方式对比 + 操作手册 |
| [docs/W14-defense-outline.md](docs/W14-defense-outline.md) | W14 多端登录答辩提纲 |
| [submission/01_DESIGN_PROPOSAL.md](submission/01_DESIGN_PROPOSAL.md) | 设计方案 (9 段标准格式) |
| [submission/02_ATTRIBUTION.md](submission/02_ATTRIBUTION.md) | 参考声明 (15 库 + 4 借鉴 + 自研分界) |

---

**5 分钟跑不起来?** 把第 4 节故障速查全部跑一遍, 或看 `docs/TEAM_SETUP.md` 第 9 节"故障排除"。
