# =====================================================
# dlib 备选安装方案
# 本项目默认走 dlib-bin（requirements.txt），
# 备选方案 1: face_recognition（自带 dlib 编译产物，1.3.0 只支持到 cp310）
# 备选方案 2: 源码编译 dlib（最慢，依赖 cmake + Visual Studio Build Tools）
# 备选方案 3: 纯 OpenCV（最简单，识别率低）
# =====================================================

# ✅ 推荐：预编译 dlib wheel（cp311/cp312/cp313）
# 已在 requirements.txt 中固定 dlib-bin==20.0.1

# 备选 1：face_recognition（含 dlib + 4 个核心函数的高级封装）
# ⚠️ face_recognition==1.3.0 在 Windows + Python 3.13 上
#    拉 dlib 子依赖时可能因 GBK 编码问题编译失败。
# face_recognition==1.3.0

# 备选 2：源码编译 dlib（最慢）
# 需要先安装 cmake 和 Visual Studio Build Tools（C++ build tools）
# pip install cmake
# pip install dlib

# 备选 3：纯 OpenCV（不依赖 dlib，识别率低）
# pip install opencv-contrib-python==4.13.0.92
# 然后用 cv2.face.LBPHFaceRecognizer_create()
